# WHEELTEC HC-05 主从一体蓝牙模块配置与项目使用指南

## 1. 先理解两种“模式”

本项目中有两类不同的模式，不要混淆：

1. HC-05 模块角色：主机或从机，通过 AT 指令设置。
2. 小车程序显示模式：蓝牙接收显示或正式选题，通过代码宏定义设置。

“主从一体”表示同一型号模块可以配置成主机或从机，并不表示一块模块会同时作为主机和从机。两块 HC-05 通信时，必须一块配置为主机，另一块配置为从机。

当前测试链路为：

```text
电脑串口助手
    |
USB 转 TTL
    |
HC-05 主机
    ))) 蓝牙 (((
HC-05 从机
    |
小车 UART1
    |
OLED 显示接收数据
```

## 2. 当前项目通信参数

当前分支中，小车 UART1 的正常透明传输参数为：

```text
波特率：115200
数据位：8
停止位：1
校验位：无
硬件流控：无
```

对应配置位置：

```text
empty.syscfg
```

当前配置代码为：

```c
UART_DEBUG_CFG.targetBaudRate = 115200;
```

SysConfig 生成的配置位于：

```text
Debug/ti_msp_dl_config.c
Debug/ti_msp_dl_config.h
```

生成结果中的实际波特率为 `115211.52`，与目标值 `115200` 的误差约为 `0.01%`。

不要直接长期修改 `Debug/ti_msp_dl_config.c` 或 `Debug/ti_msp_dl_config.h`。如果以后需要修改波特率，应修改 `empty.syscfg`，然后重新运行 SysConfig 或重新构建工程。

注意：HC-05 的 AT 配置模式固定使用 `38400 / 8N1`。正常透明传输使用的 `115200` 与 AT 模式的 `38400` 是两套不同参数。

## 3. 准备工具

配置两块模块只需要一个 USB 转 TTL，可以先配置从机，断电取下后，再用同一个 USB 转 TTL 配置主机。

需要准备：

- 两块 WHEELTEC HC-05 主从一体蓝牙模块
- 一个 USB 转 TTL，建议支持 3.3V 串口逻辑电平
- 串口助手
- 若干杜邦线

建议先给两块模块贴标签：

- 模块 A：从机，小车端
- 模块 B：主机，电脑端

## 4. USB 转 TTL 接线

配置任意一块模块时，按下表接线：

| USB 转 TTL | HC-05 |
|---|---|
| 5V | VCC |
| GND | GND |
| TXD | RXD |
| RXD | TXD |

注意事项：

- TXD 和 RXD 必须交叉连接。
- USB 转 TTL 与模块必须共地。
- 模块电源推荐使用 5V。
- 模块串口通信引脚为 3.3V 逻辑电平。
- 换模块或改变接线前，应先拔掉 USB，避免带电插拔。

## 5. 进入完整 AT 配置模式

每次配置模块都按以下步骤操作：

1. 关闭串口助手并拔掉 USB，使模块完全断电。
2. 按住 HC-05 模块上的按键不松开。
3. 插入 USB，给模块上电。
4. 继续按住按键约 2 至 3 秒。
5. 确认 LED 进入慢闪状态，通常约 2 秒闪烁一次。
6. 打开 USB 转 TTL 对应的串口。

串口助手设置为：

```text
波特率：38400
数据位：8
停止位：1
校验位：无
硬件流控：无
发送格式：ASCII 文本
行尾：CRLF
```

每次只发送一条指令，等待模块返回后再发送下一条。输入框中不要残留上一条命令。

先发送：

```text
AT
```

模块应返回：

```text
OK
```

如果串口助手显示发送字节数，`AT` 加 CRLF 应为 4 字节。如果只显示 2 字节，说明没有附加 CRLF。

部分模块需要在整个配置过程中一直按住按键，才能保持完整 AT 模式。如果 `AT` 能返回 `OK`，但其他指令无响应或返回 `ERROR:(0)`，可以保持按键按下再发送。

## 6. 配置模块 A：小车侧从机

将模块 A 接到 USB 转 TTL，并进入完整 AT 模式。

### 6.1 可选：恢复出厂设置

对于刚拿到但状态不确定的模块，建议先发送：

```text
AT+ORGL
```

模块应返回：

```text
OK
```

部分模块执行 `AT+ORGL` 后会重新初始化或退出完整 AT 状态。如果下一条指令没有响应，请将模块完全断电，再按住按键重新上电进入完整 AT 模式，然后继续配置。

### 6.2 设置从机角色

发送：

```text
AT+ROLE=0
```

返回：

```text
OK
```

### 6.3 设置从机名称

发送：

```text
AT+NAME=HC05_SLAVE
```

返回：

```text
OK
```

### 6.4 设置正常通信波特率

发送：

```text
AT+UART=115200,0,0
```

三个参数依次表示：

```text
115200：波特率
0：1 个停止位
0：无校验
```

模块应返回：

```text
OK
```

### 6.5 查询从机自身地址

发送：

```text
AT+ADDR?
```

可能返回：

```text
+ADDR:21:13:218C7
OK
```

`AT+ADDR?` 查询的是当前连接到 USB 转 TTL 的模块自身地址，不是查询另一块模块。因此查询地址时只需要从机自身上电，主机可以断电。

地址由三段组成，位数要求为：

```text
第一段：4 位
第二段：2 位
第三段：6 位
```

如果返回：

```text
21:13:218C7
```

补零后应记录为：

```text
0021:13:0218C7
```

后面配置主机绑定时，要把冒号改成逗号：

```text
0021,13,0218C7
```

每块模块的地址都不同，必须记录自己模块实际返回的地址，不能直接使用本文示例地址。

### 6.6 查询确认

发送：

```text
AT+ROLE?
```

期望返回包含：

```text
+ROLE:0
OK
```

发送：

```text
AT+UART?
```

期望返回包含：

```text
+UART:115200,0,0
OK
```

配置完成后，关闭串口助手，拔掉 USB，使模块断电。取下模块并标记为“从机/小车端”。

## 7. 配置模块 B：电脑侧主机

将模块 B 接到同一个 USB 转 TTL，并按前述步骤进入完整 AT 模式。

### 7.1 可选：恢复出厂设置

如果模块状态不确定，发送：

```text
AT+ORGL
```

如果执行后后续指令没有响应，请断电并重新进入完整 AT 模式。

### 7.2 设置指定地址连接模式

发送：

```text
AT+CMODE=0
```

返回：

```text
OK
```

`CMODE=0` 表示主机只连接指定地址的从机，连接更稳定。

### 7.3 设置主机角色

发送：

```text
AT+ROLE=1
```

返回：

```text
OK
```

### 7.4 绑定从机地址

假设从机查询并补零后的地址为：

```text
0021:13:0218C7
```

绑定命令应写为：

```text
AT+BIND=0021,13,0218C7
```

将示例地址替换成实际记录的从机地址。此时从机可以断电，因为主机只是把从机地址保存到自身配置中。

模块应返回：

```text
OK
```

### 7.5 设置正常通信波特率

发送：

```text
AT+UART=115200,0,0
```

返回：

```text
OK
```

主机、从机以及两端单片机 UART 的正常通信参数必须一致。本项目统一使用 `115200 / 8N1`。

### 7.6 查询确认

依次发送：

```text
AT+ROLE?
AT+CMODE?
AT+BIND?
AT+UART?
```

期望结果分别包含：

```text
+ROLE:1
+CMODE:0
+BIND:从机地址
+UART:115200,0,0
```

每条响应通常还会带有一行 `OK`。

配置完成后，关闭串口助手并拔掉 USB，使主机模块断电。

## 8. 正常连接和电脑发送测试

正常透明传输时不要按模块按键，也不要使用 AT 模式的 `38400` 波特率。

### 8.1 电脑侧主机接线

| USB 转 TTL | HC-05 主机 |
|---|---|
| 5V | VCC |
| GND | GND |
| TXD | RXD |
| RXD | TXD |

### 8.2 小车侧从机接线

| HC-05 从机 | MSPM0G3507 |
|---|---|
| VCC | 5V |
| GND | GND |
| TXD | PA9 / UART1_RX |
| RXD | PA8 / UART1_TX |

### 8.3 建立连接

1. 将从机插到小车上。
2. 将主机接到电脑 USB 转 TTL。
3. 不按模块按键，正常给两边上电。
4. 等待主机自动连接已经绑定的从机。
5. 连接成功后，两块模块的 LED 会进入连接状态的慢闪模式。

电脑串口助手设置为：

```text
波特率：115200
数据位：8
停止位：1
校验位：无
硬件流控：无
发送格式：ASCII 文本
行尾：CRLF
```

发送：

```text
Hello 123
```

小车 OLED 第一行应显示：

```text
BT RX 115200 8N1
```

后续行应显示收到的文本。当前程序收到 CR、LF 或 CRLF 后提交一行；每行最多 21 个字符，超过后自动换行，超过 7 行后向上滚动。

## 9. 项目中的显示模式宏

项目显示模式在以下文件中设置：

```text
app_config.h
```

可用宏为：

```c
#define APP_DISPLAY_MODE_PROBLEM_SELECT 0U
#define APP_DISPLAY_MODE_BLUETOOTH_RX   1U
```

### 9.1 当前蓝牙测试模式

当前代码默认配置为：

```c
#define APP_DISPLAY_MODE APP_DISPLAY_MODE_BLUETOOTH_RX
```

该模式用于当前蓝牙链路测试：

- OLED 显示蓝牙 UART1 收到的 ASCII 数据。
- 收到的 `STOP`、`START` 等文字只会显示，不会作为车辆控制命令执行。
- 不主动发送 VOFA 遥测数据。
- 小车循迹控制仍由原来的定时器中断运行。

### 9.2 比赛正式选题模式

题目确定、需要恢复旋钮选题和题号显示时，打开：

```text
app_config.h
```

将：

```c
#define APP_DISPLAY_MODE APP_DISPLAY_MODE_BLUETOOTH_RX
```

改为：

```c
#define APP_DISPLAY_MODE APP_DISPLAY_MODE_PROBLEM_SELECT
```

保存后重新 Build 并烧录程序。正式选题模式会恢复：

- EC11 旋钮选择题号。
- OLED 显示选中的题目。
- 按键进入对应比赛任务。

不需要修改 `car_control.c`、`line_tracker.c` 或 `competition_tasks.c` 中的循迹代码。

## 10. 常见问题排查

### 10.1 发送 AT 指令没有回复

依次检查：

1. 模块是否按住按键上电，并进入约 2 秒闪一次的慢闪状态。
2. AT 模式串口是否为 `38400 / 8N1`。
3. 是否使用 ASCII 文本发送。
4. 是否选择 CRLF 行尾。
5. `AT` 加 CRLF 是否实际发送了 4 字节。
6. TXD 和 RXD 是否交叉连接。
7. USB 转 TTL 和模块是否共地。
8. 是否打开了正确的 CH340 串口。

### 10.2 `AT` 返回 OK，其他命令返回 `ERROR:(0)`

这通常表示模块收到了命令，但没有处于完整 AT 模式，或者命令格式不符合要求。可以：

1. 完全断电。
2. 按住按键重新上电。
3. 在发送其他命令时继续按住按键。
4. 确认每条命令单独发送，输入框没有拼接上一条内容。
5. 如果刚执行过 `AT+ORGL`，重新断电进入 AT 模式后再继续。

### 10.3 `AT+ADDR?` 返回错误

`AT+ADDR?` 只能查询当前接在 USB 转 TTL 上并处于完整 AT 模式的模块自身地址。查询从机地址时，必须把从机接到 USB 转 TTL 并上电；另一块主机不需要上电。

### 10.4 两块模块无法自动连接

检查：

1. 从机是否为 `ROLE=0`。
2. 主机是否为 `ROLE=1`。
3. 主机是否为 `CMODE=0`。
4. 主机 `BIND` 地址是否使用从机的实际地址。
5. 地址是否按 `4 位,2 位,6 位` 补零并使用逗号分隔。
6. 正常连接时是否没有按模块按键。
7. 两块模块是否都已经退出 AT 模式并重新上电。

### 10.5 已连接但 OLED 没有数据

检查：

1. 电脑串口助手是否改为 `115200 / 8N1`。
2. 两块 HC-05 是否都配置为 `AT+UART=115200,0,0`。
3. 小车是否已经烧录当前 `115200` 版本代码。
4. 从机 TXD 是否连接 PA9，RXD 是否连接 PA8。
5. 是否发送了 CRLF，使 OLED 提交当前行。
6. `app_config.h` 是否仍为 `APP_DISPLAY_MODE_BLUETOOTH_RX`。

